# ApPlanner Django

Base da reescrita do ApPlanner em Python/Django para deploy via Docker/Coolify.

A branch `django-replatform` preserva o PHP legado como referência durante a migração funcional e de dados.
